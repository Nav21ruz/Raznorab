/*
 * Настройки приложения. Этот файл читается при запуске страницы,
 * поэтому его можно править прямо на хостинге — пересобирать проект не нужно.
 *
 * Заполните оба поля значениями из вашего проекта Supabase:
 *   Supabase → Project Settings → Data API (или API)
 *     Project URL  -> SUPABASE_URL
 *     anon public   -> SUPABASE_ANON_KEY
 *
 * Пока поля пустые, приложение работает в демо-режиме: данные хранятся
 * только в браузере посетителя и никуда не сохраняются.
 *
 * Ключ anon public не является секретом — он и должен находиться в коде
 * страницы. Доступ к данным ограничивают политики RLS на стороне Supabase.
 */
window.__APP_CONFIG__ = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
}
