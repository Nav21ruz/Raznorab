/*
 * Настройки приложения. Этот файл читается при запуске страницы,
 * поэтому его можно править прямо на хостинге — пересобирать проект не нужно.
 *
 * API_URL — адрес вашего сервера для основного приложения (Стройбиржа: заказы,
 * строители, разнорабочие, чат, админка). Пока поле пустое, приложение работает
 * в демо-режиме: данные хранятся только в браузере посетителя и никуда не уходят.
 * Как получить свой адрес — см. инструкцию по установке (файл
 * ЧИТАТЬ-ПЕРЕД-ЗАЛИВКОЙ.txt рядом с этим архивом).
 *
 * SUPABASE_URL / SUPABASE_ANON_KEY — отдельная настройка ТОЛЬКО для инструмента
 * "Журнал объекта" (/journal) — прораб ведёт учёт материалов и фото по объекту.
 * Это не связано со Стройбиржей и Supabase-ключ не является секретом — он и должен
 * быть в коде страницы, доступ к данным ограничивают правила безопасности Supabase.
 * НИКОГДА не вставляйте сюда ключ "service_role" — он даёт полный доступ к базе.
 */
window.__APP_CONFIG__ = {
  API_URL: '',
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
}
