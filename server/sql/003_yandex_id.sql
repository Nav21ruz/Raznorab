-- ============================================================
-- Вход через Яндекс ID (OAuth) — третий способ входа наравне с email/паролем
-- и Telegram. Как и telegram_id, это просто ещё один способ узнать
-- пользователя — отдельная учётная запись, не связанная с остальными.
-- ============================================================

alter table profiles add column if not exists yandex_id text unique;
